import React from 'react';

const Home = () => {
    return (
        <div>
            Hi, this is a tool will show the recent COVID-19 stats, just select a country from the sidebar
        </div>
    );
}

export default Home;
